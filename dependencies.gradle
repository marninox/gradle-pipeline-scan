ext {
    versions = [
        guava     : '32.1.3-jre',
        junit     : '5.10.0',
        collections : '3.2.1'  // vulnerable version
    ]

    libs = [
        guava     : "com.google.guava:guava:${versions.guava}",
        junit     : "org.junit.jupiter:junit-jupiter-api:${versions.junit}",
        collections : "commons-collections:commons-collections:${versions.collections}"
    ]
}
